spring:
  h2:
    console:
    enabled: true
    path: /h2
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE;
    driver-class-name: org.h2.Driver # H2 DB
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
